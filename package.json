{
	"name": "pi-bash-inline-completion",
	"version": "0.1.0",
	"description": "zsh-style inline ghost completion for bash mode (!commands) in the pi coding agent — with automatic dropdown fallback for multiple matches",
	"keywords": ["pi-package", "pi", "autocomplete", "completions", "shell", "zsh"],
	"license": "MIT",
	"author": "Andrew Mahoney-Fernandes <0xPlayerOne>",
	"type": "module",
	"pi": {
		"extensions": ["./extensions/*.ts"]
	},
	"peerDependencies": {
		"@earendil-works/pi-coding-agent": "*",
		"@earendil-works/pi-tui": "*"
	}
}
